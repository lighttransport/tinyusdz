#usda 1.0
# EXPECT: core.layer.metersPerUnit
# EXPECT: core.xformOp.order
# EXPECT: geom.encapsulation.nestedGprim
(
    doc = "Several violations at once: bad metersPerUnit, dangling xformOpOrder, nested Gprim."
    metersPerUnit = 0
)

def Mesh "outer"
{
    float3 xformOp:scale = (1, 1, 1)
    uniform token[] xformOpOrder = ["xformOp:translate", "xformOp:scale"]

    def Cube "inner"
    {
        double size = 1
    }
}
