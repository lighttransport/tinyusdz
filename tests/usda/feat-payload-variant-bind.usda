#usda 1.0
(
    defaultPrim = "root"
)

# Payload target: non-geometry content (a material scope), like ALab's surfacing
# look-binding. Composing this onto /root must keep /root's variant geometry.
def Xform "root"
{
    def Scope "MATERIAL"
    {
    }
}
