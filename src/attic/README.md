# src/attic — retired sources (scheduled for deletion)

Files here are no longer part of any build. They are kept temporarily for
reference and are **marked to be deleted in the future**.

- `tinyexr.{h,cc}` — legacy TinyEXR v1 API. Superseded by the pure-C11 TinyEXR
  v3 C backend in `src/external/tinyexr/` (used unconditionally by
  `image-loader.cc` / `image-writer.cc`). Retired 2026-07.
