// SPDX-License-Identifier: Apache-2.0
// Resource IDs shared between raytracer_kernel_resource.rc (compiled by the
// Windows resource compiler) and kernel_resource.cc (which looks the
// resource up at runtime via FindResource).
#pragma once

#define IDR_RAYTRACER_KERNEL 101
